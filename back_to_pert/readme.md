
The output for the pert-code is stored in a folder "bg_data" upon evaluation.

For using the plot scripts in Mathematica the parameter `debug` should be set to true in the parameter file. This stores all the Taylor coefficients for the expensions around the cutoff surfaces as well. 
